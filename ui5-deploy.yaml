# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "4.0"
metadata:
  name: oveetest.project1
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          destination: SolmanSSO
          url: http://sso:8000
          client: '100'
        app:
          name: ZTESTASSIA8
          package: ZFIORI_BAS
          transport: SSOK900081
        exclude:
          - /test/
          - /localService/
